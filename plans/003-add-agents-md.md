# Plan 003: Add `AGENTS.md` for agent orientation

> **Executor instructions**: Follow this plan step by step. Run every verification
> command and confirm the expected result before moving to the next step. If
> anything in the "STOP conditions" section occurs, stop and report — do not
> improvise. When done, update the status row for this plan in `plans/README.md`.
>
> **Drift check (run first)**: `git diff --stat 17b661a..HEAD -- README.md flake.nix modules/desktop/hyprland/default.nix .github/workflows/check.yml`
> If these changed, re-read them before writing — the AGENTS.md content must match reality.

## Status

- **Priority**: P2
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: dx
- **Planned at**: commit `17b661a`, 2026-06-17
- **Issue**: (not published)

## Why this matters

This repo is actively worked on by coding agents (this session, a `.gemini/` dir exists). Right now every agent re-derives the project layout, build/test commands, module conventions, and override mechanism from scratch by reading the whole tree. An `AGENTS.md` at the repo root — the cross-tool convention read by Codex, Cursor, pi, and others — gives any agent a correct, compact orientation in one read, reducing wrong-path edits and missed conventions. The user explicitly chose `AGENTS.md` (not `CLAUDE.md`) as the single source of truth.

## Current state

- No `AGENTS.md` or `CLAUDE.md` exists at the repo root (verified: `ls` root shows only `README.md`, `KEYBINDINGS.md`, config files).
- `README.md` already documents setup, layout, and customization accurately (paths are `modules/desktop/hyprland/...`, `shared.nix`/`local.nix`/`home.local.nix` override mechanism, fish helpers `nswitch`/`ntest`/`nupdate`/`nrollback`). AGENTS.md should **not duplicate** the user-facing README — it should point at it and add only what an agent needs: build/verify commands, module conventions, the Nix→Lua generation boundary, and hard rules (never edit generated files, never touch gitignored host files).

Key facts to encode (all verified against `17b661a`):

- **Build/verify**: `nix flake check` and `nix build .#nixosConfigurations.main.config.system.build.toplevel --no-link` (matches `.github/workflows/check.yml`). CI stubs `hosts/laptop/hardware-configuration.nix` from the `.example` if missing.
- **Languages/formatters**: Nix (no formatter configured — do not reformat unrelated files); Lua with `stylua` (`.stylua.toml`: column_width=120, Spaces, indent 2); QML (no formatter). Fish shell functions under `modules/shell/fish/functions/`.
- **Generated files — never hand-edit**: `~/.config/hypr/variables.lua` and `~/.config/hypr/scheme/{default,current}.lua` are generated by `modules/desktop/hyprland/default.nix` (attrs `variablesLua`, `schemeLua`, `hyprConfig`). `~/.config/quickshell/nesw/Fonts.qml` is generated by `modules/desktop/quickshell/default.nix` (`fontsQml`). Edit the Nix attrs, not the generated output.
- **Override mechanism**: defaults in `modules/`; machine-specific overrides in gitignored host files — `hosts/laptop/local.nix` (NixOS-only: `nesw.drivers.*`), `hosts/laptop/shared.nix` (both: `nesw.theme.*`, `nesw.desktop.hyprland.*`), `hosts/laptop/home.local.nix` (Home Manager only). Options use plain `default =` (themes) and `mkDefault` for a few override-friendly hyprland options. Never put `nesw.drivers.*` in `shared.nix`/`home.local.nix` — NixOS-only.
- **Hard constraints**: `networking.hostName = "main"` must match the flake target `.#main`. `userName` in `flake.nix` must match the real Linux user. Enable **exactly one** `nesw.drivers.{amdgpu,intel,nvidia}.enable` (enforced by an assertion in `modules/drivers/default.nix`). Flakes can't see gitignored files — `nswitch`/`ntest` force-stage them; for manual first-build, `git add -f` the host override files.
- **Module conventions**: each module dir has a `default.nix` with a `/* ... */` header comment (what it does, `Exposes:`, `Depends:`). Aggregator modules use `imports = [ ./child ... ]`. New module: add dir, add `default.nix` with header, import from parent category `default.nix`. Follow `modules/shell/starship/default.nix` as a minimal exemplar; `modules/desktop/hyprland/default.nix` as the maximal exemplar (options + generated files).
- **Rebuild helpers** (Fish, run from any dir after HM active): `nswitch` (switch), `ntest` (test, reverts on reboot), `nupdate` (update flake inputs + test), `nrollback` (previous generation). They `pushd ~/nesw` and force-stage gitignored host files.

## Commands you will need

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| File exists | `test -f AGENTS.md && echo ok` | `ok` |
| No factual path drift | `grep -n "modules/desktop/hyprland" AGENTS.md` | at least one match (sanity) |
| Build unaffected | `nix flake check` | exit 0 |

## Scope

**In scope**:
- `AGENTS.md` (create, repo root)

**Out of scope**:
- `README.md` (do not modify — AGENTS.md links to it).
- Any source file.
- A `CLAUDE.md` (user chose AGENTS.md only).

## Git workflow

- Branch: `advisor/003-add-agents-md`
- Commit: `add AGENTS.md for agent orientation`
- Do NOT push or open a PR unless instructed.

## Steps

### Step 1: Write `AGENTS.md`

Create `AGENTS.md` at the repo root. Use this exact structure and content (verify each path/command against the live repo before finalizing — they are accurate as of `17b661a`):

```markdown
# AGENTS.md

Orientation for coding agents working on nesw. Read this before editing. For user-facing setup, customization, and disaster recovery, see [README.md](./README.md).

## What this is

A NixOS + Hyprland desktop configuration, packaged as a flake. Hyprland is configured in Lua; the shell UI (notch, border, clock, launcher) is Quickshell QML. Defaults live in `modules/`; machine-specific overrides live in gitignored host files under `hosts/laptop/`.

## Build & verify

| Purpose | Command |
|---------|---------|
| Flake check (CI gate) | `nix flake check` |
| Dry-build the system toplevel | `nix build .#nixosConfigurations.main.config.system.build.toplevel --no-link` |
| Lua style check | `stylua --check <file.lua>` (config: `.stylua.toml` — column 120, 2-space indent) |

`nix flake check` is the primary gate. If `hosts/laptop/hardware-configuration.nix` is missing (it's gitignored), copy the example first: `cp hosts/laptop/hardware-configuration.nix.example hosts/laptop/hardware-configuration.nix` (CI does this automatically).

There is no unit-test suite. Correctness is verified by `nix flake check` + dry-build + `stylua --check` on Lua.

## Generated files — never hand-edit

These are written at build time from Nix. Edit the Nix source, not the output:

- `~/.config/hypr/variables.lua` — from `variablesLua` in `modules/desktop/hyprland/default.nix`
- `~/.config/hypr/scheme/default.lua`, `scheme/current.lua` — from `schemeLua` in the same file
- `~/.config/quickshell/nesw/Fonts.qml` — from `fontsQml` in `modules/desktop/quickshell/default.nix`

The Hyprland Lua tree is **copied** (not symlinked) to `~/.config/hypr/` via `runCommand` in `modules/desktop/hyprland/default.nix`. Edits to repo `config/*.lua` take effect on the next rebuild.

## Override mechanism

- Defaults: `modules/` (use plain `default =` for option defaults; `mkDefault` only where a host override is expected to win).
- `hosts/laptop/local.nix` (NixOS-only, gitignored): `nesw.drivers.*`, system settings.
- `hosts/laptop/shared.nix` (imported by both NixOS and Home Manager, gitignored): `nesw.theme.*`, `nesw.desktop.hyprland.*`.
- `hosts/laptop/home.local.nix` (Home Manager only, gitignored): `home.packages`, HM programs.

`nesw.drivers.*` is NixOS-only — never put it in `shared.nix` or `home.local.nix`. Enable exactly one of `amdgpu`/`intel`/`nvidia` (assertion enforces this in `modules/drivers/default.nix`).

## Hard constraints

- `networking.hostName` (`hosts/laptop/configuration.nix`) must be `main` to match the flake target `.#main`.
- `userName` in `flake.nix` must match the real Linux username.
- Flakes cannot see gitignored files. The fish helpers (`nswitch`, `ntest`, `nupdate`) force-stage them; for a manual first build, `git add -f hosts/laptop/{hardware-configuration,local,shared,home.local}.nix`.

## Conventions

- Every module dir has a `default.nix` with a `/* ... */` header: what it does, `Exposes:`, `Depends:`. See `modules/shell/starship/default.nix` (minimal) and `modules/desktop/hyprland/default.nix` (maximal: options + generated files).
- Aggregators: `imports = [ ./child ... ]` in the category `default.nix`. Top-level aggregator is `modules/nesw.nix` (`nesw.enable = true`).
- Adding a module: create `modules/<category>/<name>/default.nix` with a header, import it from `modules/<category>/default.nix`.
- Fish helpers live in `modules/shell/fish/functions/` and `pushd ~/nesw` before rebuilding.
- QML lives in `modules/desktop/quickshell/config/`; `shell.qml` is the entry point listing each top-level surface.

## Rebuild helpers (Fish)

`nswitch` (rebuild + switch), `ntest` (test build, reverts on reboot), `nupdate` (update flake inputs + test), `nrollback` (previous system generation). Run from any directory after Home Manager is active.
```

**Verify**: `test -f AGENTS.md && echo ok` → `ok`.

### Step 2: Sanity-check claims against the live repo

Run each of these and confirm the result matches the AGENTS.md claim:

- `grep -n "variablesLua\|schemeLua\|hyprConfig" modules/desktop/hyprland/default.nix` → matches exist.
- `grep -n "fontsQml" modules/desktop/quickshell/default.nix` → match exists.
- `grep -n "count (x: x) driversEnabled == 1" modules/drivers/default.nix` → match exists (assertion).
- `grep -n "networking.hostName" hosts/laptop/configuration.nix` → shows `main`.

If any of these fail, the AGENTS.md text is wrong — fix the text to match reality (do not change source).

**Verify**: `nix flake check` → exit 0 (docs don't affect build, but confirms no accidental edit).

## Test plan

No automated tests. Verification is: file exists, every path/command referenced in AGENTS.md resolves in the repo, build still passes.

## Done criteria

- [ ] `AGENTS.md` exists at repo root
- [ ] `grep -n "modules/desktop/hyprland" AGENTS.md` returns ≥1 match
- [ ] Every path referenced in AGENTS.md exists in the repo (manual spot-check of the 4 sanity greps above)
- [ ] `nix flake check` exits 0
- [ ] No files other than `AGENTS.md` are modified
- [ ] `plans/README.md` status row updated

## STOP conditions

Stop and report if:

- Any sanity-check grep in Step 2 fails — the build mechanism has drifted; fix the AGENTS.md text, don't change source.
- An `AGENTS.md` already exists (it shouldn't at `17b661a`) — report rather than overwriting.
- `README.md` contains a fact that contradicts what AGENTS.md should say (e.g. a different build command) — reconcile toward `README.md`'s user-facing commands and report the discrepancy.

## Maintenance notes

- When the build/verify commands change (e.g. a test harness is added, or `nix flake check` is replaced), update the "Build & verify" table first.
- When a new generated file is added, add it to the "Generated files" list.
- Reviewer: confirm AGENTS.md doesn't duplicate README prose — it should orient an agent, not re-teach setup.
