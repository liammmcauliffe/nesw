{ ... }: {
  # Put machine-specific or experimental overrides here.
  # This file is imported by hosts/laptop/home.nix when hosts/laptop/local.nix exists.
  #
  # Examples:
  # home.packages = [ pkgs.somePackage ];
  # programs.git.enable = true;
}
