{ ... }: {
  # Copy to local.nix
  # Optional

  # Identify your GPU: nix-shell -p pciutils --run "lspci -k | grep -A 3 -E 'VGA|3D'"
  # nesw.drivers.intel.enable = true;    # Intel (i915/xe)
  # nesw.drivers.amdgpu.enable = true;   # AMD
  # nesw.drivers.nvidia.enable = true;   # NVIDIA proprietary

  # other NixOS-only system overrides:
  # boot.kernelModules = [ "kvm-intel" ];
  # environment.systemPackages = with pkgs; [ libva-utils ];

  # git add -f hosts/laptop/local.nix before rebuilding.
}
